import PortfolioStyles from './portfolio.module.scss';

const Portfolio = () => {

    return (
        /* html */`
        <h1 class="${PortfolioStyles.back}">    
            Portfolio
        </h1>
    `);
} 

export default Portfolio;

